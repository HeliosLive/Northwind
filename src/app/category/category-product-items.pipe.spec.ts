import { CategoryProductItemsPipe } from './category-product-items.pipe';

describe('CategoryProductItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryProductItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
